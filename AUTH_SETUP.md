# Supabase 초대 전용 인증 설정

야구일기 V7은 로그인 필수로 동작합니다. 아래 설정을 완료하기 전에는 실제 사용자를 초대하지 마세요.

## 1. 공개 설정 파일 확인

`supabase-config.js`에는 다음 두 값만 입력합니다.

```js
window.BASEBALL_SUPABASE_CONFIG = {
  url: 'https://프로젝트-REF.supabase.co',
  publishableKey: 'sb_publishable_...'
};
```

- Publishable/anon key는 브라우저에 노출되는 용도의 키입니다.
- `service_role` 또는 `sb_secret_...` 키는 절대 넣지 않습니다.
- 데이터 접근은 공개 키를 숨기는 방식이 아니라 로그인 세션과 RLS로 제한합니다.

## 2. 데이터베이스 보강

- 기존 V6 사용자: `migration_v7.sql`만 실행
- 신규 프로젝트: `migration_v6.sql` 실행 후 `migration_v7.sql` 실행

SQL은 Supabase Dashboard의 SQL Editor에서 전체 실행합니다.

## 3. 신규 가입 차단

Supabase Dashboard의 Authentication 설정에서 신규 사용자 가입 허용을 끕니다.

- 기존 로그인 사용자는 계속 로그인 가능
- Dashboard의 관리자 초대는 계속 사용 가능
- 앱에는 계정 만들기 버튼이 없음
- 누군가 프런트엔드 API를 직접 호출해도 일반 신규 가입이 거부되어야 함

설정 후 초대받지 않은 새 이메일로 가입 API가 실제로 거부되는지 한 번 확인하세요.

## 4. URL Configuration

Authentication의 URL Configuration에서 실제 GitHub Pages 주소를 입력합니다.

예시:

```text
Site URL
https://GITHUB-ID.github.io/REPOSITORY/

Redirect URLs
https://GITHUB-ID.github.io/REPOSITORY/
```

주의:

- `github.com/...` 저장소 주소가 아니라 실제로 앱이 열리는 Pages 주소를 사용합니다.
- 저장소 하위 경로와 마지막 `/`까지 실제 주소와 맞춥니다.
- `localhost`가 남아 있으면 아이폰 초대 링크가 네트워크 오류처럼 보일 수 있습니다.
- 초대·비밀번호 재설정 모두 같은 주소로 돌아옵니다.

## 5. 사용자 초대

Authentication의 Users 화면에서 사용자의 부모/보호자 이메일로 초대를 보냅니다.

사용자 흐름:

1. 새 초대 메일의 링크를 Safari에서 열기
2. 야구일기의 `초대를 마무리하세요` 화면에서 비밀번호 설정
3. 홈 화면 PWA가 별도 로그인 상태라면 홈 화면의 야구일기를 열어 이메일/비밀번호로 첫 로그인
4. 이후 로그인 상태 유지

이미 보낸 링크에서 오류가 났다면 URL 설정을 고친 뒤 새 초대를 보내세요. 만료된 기존 링크는 다시 사용하지 않습니다.

## 6. 비밀번호 재설정

로그인 화면에서 초대받은 이메일을 입력하고 `비밀번호를 잊으셨나요?`를 누릅니다.

- 메일 링크가 앱으로 돌아오면 새 비밀번호 화면이 표시됩니다.
- 링크가 만료되면 로그인 화면에서 새 메일을 요청합니다.
- 메일 앱 내장 브라우저에서 문제가 있으면 링크를 Safari에서 엽니다.

## 7. 메일 발송

소수 테스트 이후 실제 사용자를 운영할 때는 Supabase Auth에 자체 SMTP를 연결하세요. 기본 발송 제한이나 스팸 분류 때문에 초대·재설정 메일이 지연될 수 있습니다.

## 8. 배포 후 필수 시험

### 계정 A

1. A 로그인
2. 선수 이름과 기록 1건 추가
3. 오프라인에서 기록 1건 추가
4. 온라인 복귀 후 동기화 대기 0건 확인
5. 로그아웃

### 같은 기기의 계정 B

1. B 로그인
2. A 선수·기록·분석이 보이지 않는지 확인
3. B 기록 추가
4. B 백업 내보내기
5. A 백업 불러오기가 차단되는지 확인

### 다시 계정 A

1. A 로그인
2. A의 오프라인 기록이 남아 있는지 확인
3. B 기록이 보이지 않는지 확인

### 캐시 삭제

1. 미동기화 기록이 있을 때 `로그아웃하고 이 기기 캐시 삭제`가 차단되는지 확인
2. 로컬에만 보존된 기존 비정상 행이 있으면 캐시 삭제 전 안전 백업이 내려받아지는지 확인
3. 동기화 완료 후 캐시 삭제
4. 다시 로그인했을 때 서버 기록만 정상 복원되는지 확인

## 9. 운영 원칙

- 유소년 선수는 부모/보호자 계정 아래 선수 프로필로 관리하는 방식을 권장합니다.
- 계정 삭제 요청을 처리할 때 Supabase Auth 사용자를 관리자 화면에서 삭제하면 관련 데이터는 FK cascade로 삭제됩니다.
- 삭제 전 사용자에게 최신 JSON 백업 여부를 확인합니다.
- 공개 저장소에는 Publishable/anon key만 있는지 배포마다 확인합니다.
